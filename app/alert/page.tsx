"use client";

/** STUB — see CodeBuddy_Prompts.md prompt #5. Day 1 renders inline on Home. */
import Link from "next/link";
export default function AlertPage() {
  return (
    <main className="min-h-screen bg-cream flex items-center justify-center p-6">
      <div className="max-w-md text-center">
        <h1 className="text-2xl font-bold text-navy mb-3">Family Alert page · TODO</h1>
        <Link href="/" className="text-navy underline">← Back to home</Link>
      </div>
    </main>
  );
}
